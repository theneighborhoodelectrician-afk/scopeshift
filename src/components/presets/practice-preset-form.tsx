import { Card } from "@/components/ui/card";

export function PracticePresetForm() {
  return (
    <Card className="space-y-3">
      <p className="text-xs font-semibold uppercase tracking-[0.2em] text-ember">Practice Preset</p>
      <p className="text-sm text-slate">Preset builder placeholder for targeted practice filters.</p>
    </Card>
  );
}
